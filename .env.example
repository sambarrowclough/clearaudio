# ClearAudio - Root Environment Variables
# 
# This project uses per-app environment files:
#   - apps/web/.env.local      (Next.js frontend)
#   - apps/engine/.env         (Python backend)
#
# Copy the example files to get started:
#   cp apps/web/.env.example apps/web/.env.local
#   cp apps/engine/.env.example apps/engine/.env
#
# Required tokens:
#   BLOB_READ_WRITE_TOKEN  - Get from Vercel Blob storage settings
#                            https://vercel.com/docs/storage/vercel-blob
#   MODAL_TOKEN_ID         - Get from https://modal.com/settings
#   MODAL_TOKEN_SECRET     - Get from https://modal.com/settings
#
# Modal Secret (separate from env vars):
#   Run: modal secret create huggingface-secret HF_TOKEN=hf_your_token_here
#   Get token from: https://huggingface.co/settings/tokens
